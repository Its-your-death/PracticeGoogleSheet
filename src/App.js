import React, { Component } from 'react';
import Comment from './components/Form.jsx'
import './App.css';
//import { useState, useRef, useEffect } from 'react';
//import { t, jt } from 'ttag.macro';

//import Success from './components/Enter.jsx';








function App() {
  /* function ValidPhone(){
      var output;
      var check = /^\d[\d\(\)\ -]{4,14}\ds/;
      var phone = document.getElementById('phone').Value;
      var valph = check.test(phone);
      if (valph) output ='Номер телефона верный';
      else output = 'Номер не верный';
      document.getElementById('nomber').innerHTML = document.getElementById('nomber').innerHTML + <br /> + output;
      return valph; 

  } */

    {
    return( 
        <Comment/>
        );
    }
}
export default App;
